const firstName = "amira"
const lastName = "abdelwahab"
const city = "mansoura"

function mul(x , y ){
    console.log(x*y)
}

module.exports = {
    fname : firstName,
    lname : lastName,
    city : city,
    fun1 : mul
}
